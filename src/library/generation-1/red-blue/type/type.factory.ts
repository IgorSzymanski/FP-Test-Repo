import { FactoryID, FactorySpecificIDs } from '~/library/storage/storage.factory'
import { TypeData } from './type.storage'

export const addType = FactoryID(TypeData)
export const addTypes = FactorySpecificIDs(TypeData)
