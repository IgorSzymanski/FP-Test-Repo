// @ts-nocheck

// import { PokemonEncounters } from './library/generation-1/red-blue/pokemon/data/pokemon.encounters'
// import { mapObject } from './library/helpers'
// import { IEncounter } from './library/generation-1/red-blue/pokemon/pokemon.interface'
// import { InputType } from './library/storage/storage.factory'

// const flat = (lol: Array<any>) => [...new Set(lol)]

// console.log(mapObject((lol: InputType<IEncounter[]>) => flat(lol.map(e => e.locationArea)))(PokemonEncounters))
